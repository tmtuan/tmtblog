<h1>Posts</h1>
<% @posts.each do |post| %>
    <dib class="post_wrapper">
      <h2 class="title">
        <%= link_to post.title, post %>
      </h2>
      <p class="date">
        <%= post.created_at.strftime("%B, %d, %Y") %>
      </p>
    </dib>

<% end %>